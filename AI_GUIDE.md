# AI 协作指南

沿用既有项目的“项目级规范 + REQ 需求目录 + 进度与验证记录”方式。

1. 新任务先明确目标、范围、约束、交付物和验收方式。
2. 让 AI 阅读 `AGENTS.md` 和当前需求目录，先核对实际实现。
3. 小调整可直接实施；涉及模型、执行状态、插件契约或存储的工作先写清设计与影响，再按本次授权推进。
4. 交付时检查实际 diff、验证证据，以及 `docs/progress.md` 是否与代码一致。
5. 下一次对话从需求目录继续，不依赖历史聊天才能理解项目。

本文件描述研发协作。产品自身也包含 REQ-003 的 AI 步骤编写：生成 step content、调试并保存固化。插件提供提示词和能力上下文；虚拟机内执行固定内容无需连接模型。此能力与研发协作、运行时 AI 动作分别定义。

请求模板见 `TASK_TEMPLATE.md`。当前初始化已完成的范围与下一步见 `docs/project-plan.md`。
