# AI 任务模板

## 分析

```text
阅读 AGENTS.md、docs/architecture.md、docs/progress.md 和相关 REQ 目录。
目标：分析 <问题>。
范围：<模块与文件>。
约束：本轮仅分析，不改运行代码。
输出：实际行为、代码映射、问题、最小改动方案、验证方式。
```

## 实施

```text
阅读 AGENTS.md 和 docs/requirements/<REQ>/。
目标：实现 <能力>。
范围：<核心/插件/入口/存储>。††††
约束：<兼容性、离线运行、依赖边界>。
验收：<可观察结果与运行命令>。
交付：代码、需求进度、项目进度、验证证据、未完成项。
```

## 评审

```text
评审 <分支或 diff>，结合对应 REQ 文档。
重点：状态流转、重复执行、插件隔离、上下文和旧数据兼容。
先列有证据的问题及位置，再给结论和验证缺口。
```
