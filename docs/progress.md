# 当前进度

2026-09-16：需求规划已整理，正式需求为 REQ-002 至 REQ-009，范围见 [总需求池](requirements/REQ-001-requirement-pool/backlog.md)。

REQ-003 支持 AI 与手动编写步骤；REQ-004 实现插件机制和 Playwright 验证案例。当前仅调整规划，运行功能未实现。

2026-09-16：核对旧 Step 无版本；采用总库配置/执行记录与任务库可选业务结果分离。Playwright 结果按需，插件 AI 扩展覆盖工具、上下文、校验和诊断，不限提示词。仅调整设计。

2026-09-16：按已确认对话统一各 REQ 职责，补齐结果保存的插件分工，移除图形编辑文档残留的步骤版本要求。范围已确认，详细契约与功能实现尚未完成。

## REQ-002 设计交付，待用户评审 · 2026-09-16

详细契约、DDL、接口和示例已交付，8 项设计检查通过。旧代码仅参考，不强制兼容。待用户确认设计后再进入 REQ-003；真实产品功能未实现。

UI 方案确认采用 Windows 独立客户端；已补充 Playwright/Chromium 内置及 WebView2 离线依赖方案。尚未生成安装包或完成 Windows 实机验证。

交付约束更新：目标机禁止安装，只允许执行 exe；已改为免安装便携包方案，取消安装器及系统依赖补装。窗口后端需按目标 Windows 环境验证。

目标环境已确认 Windows 10 x64；业务浏览器默认内置匹配 Playwright 的 Chromium，系统 Chrome 仅作为验证后的可选通道。

2026-09-16：评审调整：步骤返回数据默认落任务库，后续步骤从 DB 读取；去除选择性保存开关。核心统一存储，插件定义自定义结果、表结构及转换/解析。REQ-002 等待用户确认，尚未实现产品功能。

## REQ-003 可运行交付 · 2026-09-17

REQ-002 已确认。REQ-003 交付 CLI、本地 API、任务与步骤核心、DB 结果引用、worker 和可选 AI 编写。五步 demo 与独立 HTTP 流程已通过，实际模型、Windows、Playwright 未验证。详见 [验收记录](requirements/REQ-003-task-core/validation.md)。

## REQ-004 插件与 Playwright · 2026-09-17

交付标准插件发现/启停、SDK、独立浏览器与 sample 包、AI 编写扩展、当前页面观察、失败截图和核心表迁移。真实 Chromium 模拟订单示例已通过，结果默认落任务库并由后续步骤读取，业务提交次数为 1。详情见 [验证记录](requirements/REQ-004-plugin-contract/validation.md)。Windows/真实内网仍待实机交付验收。

2026-09-17：REQ-005 调整为 Windows / macOS 跨平台独立客户端，共用 UI 和应用服务，分平台验证与打包；Windows 10 x64 离线免安装约束保持。

2026-09-17：REQ-005 已完成代码实现并进入最终回归：跨平台任务内界面与核心执行间隔已落地。macOS 原生窗口与实际 UI 流程通过，Windows/Apple Silicon/冻结包待各自实机验收。运行见 [REQ-005 指南](requirements/REQ-005-local-workbench/usage.md)。

2026-09-17：断电后恢复完整验证，52 项测试全部通过（269.131 秒），包含核心、Playwright、间隔恢复和真实界面操作。工程校验、8 项设计检查、core 与 Playwright wheel 构建均通过。REQ-005 源码客户端交付，待用户验收；Windows / Apple Silicon 实机与冻结包仍未验证。

2026-09-17：修复默认启动端口检测。当前机器 localhost IPv4 bind 实测报 Errno 49，而 127.0.0.1 正常；取消 NiceGUI 的 localhost 固定范围探测，改由系统分配 IPv4 回环端口。REQ-005 12 项针对性检查通过（25.222 秒）；未指定 --port 的 macOS 原生窗口实际启动，回传 native_window_ready=true、size=[1280,850]。此前 52 项完整回归记录保持为修复前证据。

2026-09-17：补齐 AI 服务适配与独立实时日志窗口。DeepSeek 使用非思考请求参数；解析兼容 nullable tool_calls、JSON 围栏，拒绝空/截断/缺失步骤内容，连接测试检查 async run 语法。默认模型保持 Chat Completions，未新增 Responses/Anthropic 协议。

顶部服务日志按钮在 native 模式打开独立 pywebview 子进程，浏览器检查模式新开标签；日志经 NiceGUI Socket.IO 推送，支持筛选、暂停和下载，保存在 home/logs/server.log。记录 Python logging 与服务操作阶段，worker 业务日志继续在任务页面；不宣称已收集全部 stdout/stderr。服务退出回收日志窗口。

3 项模型/日志检查通过；12 项 REQ-005 回归通过（28.127 秒）；真实界面验收通过（96.504 秒），包含 nullable tool_calls、日志新窗口与新增 AI 请求实时追加。macOS native 主窗口启动、日志按钮拉起独立窗口子进程并随服务退出回收已验证。真实用户密钥仍在用户进程会话内，未验证 DeepSeek 账户生成；此前无密钥 HTTPS 检查只证明 TLS 正常。

2026-09-17：真实 Chromium 新用例通过（13.491 秒），验证同任务五步调试共用 PID/页面、正式运行到第三步后继续到第五步只产生五次尝试、成功后仍保留进程、显式结束后资源释放且历史保持成功。完整 60 项检查（255.042 秒）中 58 项通过，包括真实 UI；两项测试在启动后修订导致运行进程仍使用旧测试内容：新增用例首步骤无业务输出，及旧模型桩少一次修正响应。修订后分别单独通过（13.491 秒、0.357 秒），实现未因此更改。日志在 .runtime/retained-browser-validation.log，不能将该轮日志标为完整通过。工程与 diff 检查通过。

2026-09-17：真实浏览器超时失败后，经 DesktopController 关闭资源的检查通过（9.869 秒）；反馈包含最近尝试错误及日志。真实界面回归通过（91.967 秒）。按用户追加要求，AI 请求对话及回复正文打印到服务日志，仍不记录 Authorization 头，API Key 脱敏；7 项模型/编写/日志检查通过（1.475 秒），覆盖对话可见和认证头不泄露。该记录取代此前不记录模型正文的约定。

2026-09-17：修复重启执行器残留锁：持有协调器 OS 锁后清除旧 runtime_lease，正常退出也释放占用；历史运行与 UNKNOWN 尝试保留，不自动判定副作用。步骤反馈重开时从 DB 恢复最新调试，重新显示结束入口。新调试创建前检查占用，同任务同环境已知安全失败允许重新调试，未知副作用必须先结束/核对，不新增被阻塞的孤立调试记录。12 项 REQ-005 回归通过（18.068 秒），新增死 owner 残留锁重启检查通过（1.574 秒），结果提交恢复检查通过（1.803 秒）；工程及 diff 检查通过。

2026-09-17：新增补充说明、修复建议采纳落库的真实界面检查最终通过（127.284 秒）；验证补充说明进入模型请求、采纳源码存入 steps 且状态为 DRAFT、日志实时追加。9 项模型/历史/权限/日志/最新草稿执行检查通过（3.048 秒）。首次新增界面检查最后因日志成功记录不再唯一而失败，调整测试定位器后通过；实现未因此更改。工程与 diff 检查通过。

2026-09-18：用户五项调整已实施：长行日志换行；正式执行横向列表、新建执行选择环境、步骤点击三选项以及仅清理本次结果从头执行；环境名称+Key/Value、角色/凭据统一变量；Playwright 从任务参数读取并提供配置变量说明；代码差异由程序生成；增加手动确认成功保存和验证来源。总库 v4 备份迁移。新增 4 项核心/重启/环境/参数检查通过（4.193 秒），v2→v4 迁移检查通过（0.850 秒）。完整回归执行中。

2026-09-18：五项调整完整回归通过：69 项（282.904 秒），含真实 NiceGUI/Chromium 界面、新建执行及点击步骤继续、通用环境编辑、手动确认与本次执行重置隔离、Playwright 任务参数优先级。工程检查与 REQ-002 的 8 项设计检查通过，git diff --check 通过。日志见 .runtime/workbench-sept18-validation.log。未进行 Windows 便携包验证。

2026-09-18：正式执行按钮统一为“结束执行”，调试为“结束调试”。点击先确认，按执行定义快照与插件资源声明展示将释放的资源；Playwright 提供可选 resource_descriptions 说明浏览器/页面/上下文，其他插件无说明则显示资源 ID。仅关闭已创建资源，无资源则提示释放执行器。取消不发送控制命令，保存结果与历史保留。工程检查通过，4 项相关运行/重置/配置回归通过（3.647 秒）；本轮未重跑完整 UI 集成。

2026-09-18：日志窗口删除浏览器式下载入口，改为打开本地日志目录；新增复制当前显示日志（macOS pbcopy / Windows clip，Linux 浏览器剪贴板），清空与筛选同步维护显示缓冲，并开放文本选择。日志始终自动保存到 logs/server.log。工程检查与 9 项模型/日志回归通过（3.584 秒）；未验证 Windows 系统剪贴板或重跑原生窗口。

2026-09-18：按用户简化要求，移除“复制当前日志”按钮及系统剪贴板调用，仅保留“打开日志目录”；清空、级别筛选、暂停和可选中文字仍保留。

2026-09-18：普通 AI 生成/修订不携带或写入调试历史，也不自动附带调试反馈；日志修复显式 use_history=True，累计当前调试过程的修复对话。反馈页再次执行调试沿用上一调试环境和实际输入、保存并执行最新草稿，不弹环境确认且不清空历史。内容页发起新的调试仍重置调试历史，内存按步骤隔离。新增 use_history/continue_session 均可选，默认无历史/新调试，兼容旧接口。9 项相关模型/历史/草稿回归通过，工程检查通过。

2026-09-18：添加步骤默认继承任务末尾步骤的全部插件，并展开当前插件完整能力；先保存当前编辑再读取最新步骤，避免丢失刚调整的插件。首步骤仍默认无插件，内容/输入/验证状态不复制，新步骤可自行调整插件。

2026-09-18：上下文观察支持成功调试/正式执行的保留会话，成功验证保存后可供后续步骤采集。新增 run.context.sessions 仅返回本进程实际存活、当前 task 匹配且未执行中的保留会话；重启自动为空，不把 DB 历史作为会话。采集接口允许 SUCCEEDED 无租约，但强制校验 session_run_id，避免旧运行误读新浏览器。

2026-09-18：修复失败调试阻挡修订：仅步骤保存允许空闲 FAILED/SUCCEEDED TRIAL 租约，不释放浏览器，不放开正式运行或执行中编辑锁。反馈页显式重试允许同任务环境失败调试租约移交到新尝试，保留旧 UNKNOWN/FAILED 证据；效果未知时提示用户检查页面并确认再次执行，不强制结束浏览器，记录 USER_RETRY_WITH_RETAINED_SESSION。真实 Chromium 超时失败→最新草稿修订→同 PID/页面再调试成功→显式结束检查通过（11.588 秒）。工程检查通过。

2026-09-18：调试反馈页新增“从首步开始”：确认环境/任务输入后关闭当前同任务调试资源，新建 flow_trial（TRIAL 的可选 request_json 标志，无 DDL 变更），执行第一步到当前步骤，使用正式绑定及数据库前序结果和间隔，失败停止，不要求预先成功验证；历史调试保留。普通反馈页继续调试仍保留页面。6 项相关回归通过（6.918 秒），包含草稿前序依赖及全新 worker 检查。

2026-09-18：按确认方案实施 Playwright 四项优化：可见控件快照/可靠定位与状态、输入值及 URL/标题/文字结果等待、动作开始/失败/耗时与失败快照、修复前自动刷新保留会话、插件专属提示词。升级插件到 0.2.0、API v1 不变、无 DDL 变更。新增真实 Chromium 连续搜索检查通过（11.097 秒）：前步打开页面→隐藏旧控件失败→新快照进入模型请求→修订使用可见控件→同 PID 输入点击及实际结果断言成功，再次采集为新时间及结果页面。原人工提交场景单独回归通过（31.552 秒）。完整回归执行中。

2026-09-18：Playwright 四项优化完整回归 72 项通过（339.693 秒），包含真实 NiceGUI/Chromium 界面、插件、运行/存储。运行中追加的历史快照引用优化另以模型/历史 10 项通过（6.001 秒），最终快照与无租约 READ 失败继续采集另以真实连续搜索用例通过（9.481 秒）。工程检查、设计 8 项检查及 diff 检查通过。浏览器搜索使用本机测试站点，AI 使用模型 fixture，未声称真实 DeepSeek/百度端到端成功。首次插件回归发现普通元素不可调用 is_editable、LOCATOR 诊断范围及旧测试误把 selector.value 当成业务输入值；修正后完整回归通过。

2026-09-18：结束按钮改为 run.get 的实时 can_end（当前存活 worker/session 或当前运行租约），不按 SUCCEEDED 历史状态推断。显式结束后的成功历史、未启动执行和重启后历史不显示按钮；保留会话或占用执行器时显示，调试结束入口同规则。核对结果仍需选择已完成/未完成，说明改为选填，后台允许空 evidence，不取消人工核对。7 项相关检查通过（22.133 秒），未知执行人工核对空说明后继续检查通过（2.637 秒）；工程与 diff 检查通过。无数据库迁移，can_end 是实时派生字段。

2026-09-18：运行历史、调试反馈和运行日志改为步骤序号及执行时名称；执行列表与上下文会话使用执行类型、时间、环境及状态，不展示内部 UUID。历史优先使用 definition_json 快照，旧记录回退当前步骤，无法解析显示未知步骤。仅转换界面基础设施元数据，业务结果、存储、接口与服务日志不变，无数据库迁移。

2026-09-18：新增 AI Chat 生成/修订与 AI Chat 根据历史修订入口，无模型配置亦可导出项目规范、插件能力、上下文/最新日志/可选调试对话及补充说明。粘贴网页回复 JSON 或 Python 代码，采纳保存草稿并重绘当前页，保留反馈页继续调试入口及环境。历史修订采纳回合仅保存内存；普通导出不带历史、不增加调试对话。插件 AuthoringContribution 新增可选 channel_overrides（api/web_chat，instructions/examples），默认空保持现有插件兼容；Playwright 提供网页专属规则，禁止假装调用本机工具。无 DDL 变更。

2026-09-18：按用户要求允许本地环境配置与任务参数直接保存普通密钥值，摘要和 AI 提示继续脱敏；不修改操作系统环境变量，原 env: 引用可选兼容。步骤 inputs 自动合并环境变量、任务默认/运行参数与显式绑定/输入；显式值优先，封闭 schema 过滤未声明变量。任务配置保存独立于步骤表单，保留未保存步骤；空白参数行忽略、重名提示具体名称。AI 提示提供变量名称/来源/类型，不发送默认值；超长初始请求仅携带最近两轮历史，重复失败快照改为引用，最新上下文保留；仍超限明确提示减少采集内容。新增变量 3 项和网页/超长历史 3 项通过。

2026-09-18：新增独立 ocr 插件，使用随包 ddddocr 1.6.1/ONNX Runtime 1.20.1 本地 CPU OCR，模型和识别在 worker 单线程执行，无网络或 API Key；识别普通图片文字，返回候选文字并要求业务验证。Playwright 新增通用 page_element_image 采集元素 PNG Base64，核心不导入 OCR 或按插件硬编码。插件对 API/网页渠道提供提示词；验证码图片、输出长度和模型初始化失败有明确错误，不自动刷新或无限重试。Windows 10 x64 免安装包与真实业务验证码准确率待实机验收。

2026-09-18 最终交付：本地变量和表单自动预填、独立保存任务配置、网页 AI 两个入口及回复采纳、插件渠道适配、历史名称、超长历史保留最近两轮、本地 ocr 插件与通用元素采图均已实施。变量 5 项（1.798 秒）、执行回归 7 项（6.324 秒）、网页/中文/超长历史 4 项（1.363 秒）通过；核心 26 项、模型 10 项、显示 3 项、插件契约 5 项、OCR 3 项、连续搜索 1 项及真实界面 1 项检查通过。工程检查覆盖 101 个 Markdown 文档并通过，diff 检查通过。实际 OCR 与浏览器登录使用本机测试图片/站点；未操作用户真实登录页，未验收 Windows 便携包。无 DDL 变更；Playwright 0.3.0、ocr 0.1.0、插件 API v1。

2026-09-18：REQ-005 调整调试变量为环境/任务/步骤三组，修复切环境和重试输入失效；步骤及执行列表改为独立滚动区域、固定操作按钮、颜色图标状态。真实 UI 流程与相关变量/执行回归通过，分平台原生窗口和冻结包仍按既定范围另行验证。


### 执行重跑与参数补录（2026-09-18）

- 删除任务入口位于任务列表，确认后删除；任务配置只负责编辑和保存。
- 步骤验证不要求与执行环境匹配，修改环境不使步骤重新变成草稿。环境提供变量，不作为验证通过的条件；已创建执行仍保留配置快照一致性检查。
- 点击已成功步骤，可选择开始步骤，执行到点击的步骤。清理开始步骤及其后续输出、尝试和步骤本次输入，前面的成功结果保留，其他执行不受影响。中间重跑复用当前运行资源；从头重跑清理全部结果并重新建立资源。
- 每次执行的详情在该执行行下方展开或收起，步骤横向滚动，操作按钮固定在右侧。
- 缺少任务或步骤必录参数时，在对应执行点暂停并弹窗补录。调试反馈继续展示环境、任务和步骤三类变量，必录字段标注“必录”。取消补录保持暂停，可再点击补录按钮继续。
- `run.restart` 新增可选 `start_step_id`，旧调用仍从头执行，无数据库迁移。选择中间起点时，其之前的步骤必须已成功，避免悄悄执行未选择的前置步骤。


### 执行卡片清理与一致的补录界面（2026-09-18）

每次执行卡片右上角提供 × 删除入口，确认后仅清理该次执行、日志和结果，其他执行与任务保留。正在运行的步骤须结束后删除，保留资源的完成运行删除时释放资源。增加 `run.delete` 接口，无 DDL 变更。删除执行列表外的独立详情容器。仅在选中的执行内部创建详情；收起或切换执行时删除原容器，不保留隐藏面板。

执行调试、从首步开始、结束测试统一放在反馈顶部的一列；状态成功绿、失败红、普通灰，不在每次尝试下重复结束按钮。全局普通按钮采用中性色，移除蓝色底色。

独立调试弹窗采用自身选中的环境和对应变量；开始后同步反馈页的环境及本次输入，后续调试使用这次选择，避免重新使用页面旧环境。任务/步骤分组默认仅展开有必录输入的部分。执行补录弹窗复用同一分组界面，固定显示本次执行环境、任务变量与当前步骤变量/依赖，依赖结果读取本次运行的 DB；默认展开当前需要补录的分组，其他分组只读。


### 默认环境和首步补录优化（2026-09-18）

环境列表可设为默认，默认项左上角显示“默认”；默认环境 ID 存入本地 workbench.json，重启保留并作为首次环境选择。任务列表删除按钮放在最右侧。普通按钮白底蓝字；优先/选中操作浅青底、青绿边框，不使用蓝色底。执行删除 × 红色，与其他操作留出间距。

执行详情在所属执行内部展开，头部提供“收起详情”，执行行的详情/收起开关仍保留。补录弹窗放在稳定页面容器下，每个 InputRequested 生成唯一请求 ID，同一请求刷新不重建、不重复弹出，稍后填写可手动重开。任务参数和首步可编辑参数在首次补录中共同输入，提交时原子保存任务输入及首步 step_inputs；run.inputs 新增可选 step_inputs，旧调用保持兼容，无 DDL 变更。后续步骤到达时仍按需补录。

### 2026-09-18 输入与操作布局统一

- 运行输入显示已有值或默认值；未配置时输入框保持空白，灰色提示“未配置默认值，请录入”，不再重复显示未配置的步骤默认值说明。
- 调试三个操作横排，统一白底描边；成功使用浅青色，失败使用浅红色，与其他操作按钮保持一致。
- 对话框及页面中的相邻操作按钮横排，在窄窗口自动换行。

- 步骤确认合并为一个“确认验证并保存”按钮：当前内容有成功调试证据时直接确认；没有有效证据时弹窗询问是否手动验证通过，取消则保留草稿。

- 调试反馈的 API 与 Chat 修复入口合并为上方“AI 修复”，与三个运行操作横排；点击后选择 API 或 Chat 网页修复，沿用日志及补充说明流程。

- 2026-09-19：结果展示声明 v1、插件 result_views 扩展、Playwright 截图展示、执行列表直接查看和详情状态图标已实现；验证记录见 REQ-005。

### 2026-09-19 TiDB 与任务配置分享

- 可选 database extra 提供 `plugins/tidb`，使用 PyMySQL/MySQL 协议，连接配置读取环境及任务变量，提供连接测试、表结构采集、参数化只读查询和 tidb.table/tidb.verification 展示器。数据库连接每次关闭；API/Chat 共用插件格式，Chat 不假装可以访问数据库。
- 任务列表提供导出 JSON、复制 JSON、下载 JSON，以及粘贴导入为新任务。`taskweave-task-1` 包含任务名称、说明、参数默认值、按序步骤内容/参数/插件要求/绑定；前序依赖通过可移植步骤 key 重映射。环境、历史、文件和执行结果不导出。导入执行静态检查和插件校验，不运行步骤，全部标记 MANUAL/VALIDATED；缺失插件或无效配置拒绝导入，失败不留下半个任务。只支持当前线性步骤配置。
- 环境列表支持确认后删除，保留历史业务结果，历史执行保存 deleted_environment_name 并解除外键；保留资源、运行或暂停中的环境禁止删除。默认环境删除后清除默认设置。环境不提供导入导出。


2026-09-19：修复截图展示引用兼容，补齐 API/Chat 截图持久化提示及丢弃输出静态检查；35 项回归通过，未保存的旧图片需重新调试。


### 2026-09-19 任务一键清理

任务列表新增「清理运行」，确认后保留任务配置、参数、步骤与验证状态，删除该任务全部正式执行/调试记录、结果引用、事件和命令记录、任务结果数据库、截图文件、staging 与插件迁移备份，并清理调试对话。保留会话关闭；正在执行步骤时拒绝操作，防止写入冲突。其他任务和环境不受影响。应用公共 server.log 是服务日志，不属于单个任务运行数据，仍保留。新增 task.clear_runs 接口，无 DDL 变更。清理后可重新创建执行。


2026-09-19：结果窗口仅按步骤 views 声明生成展示页签，删除自动文件页签逻辑。保存但未声明展示的文件不自动展示；原始 JSON 保留。不改动结果存储或已有步骤。


2026-09-19：修复任务导出复制和桌面下载。macOS 桌面通过系统剪贴板写入 UTF-8，成功后提示；浏览器复制等待实际操作结果，失败显示错误。网页 AI 对话复制共用此路径。桌面「下载 JSON」保存到应用数据目录 exports 下独立 JSON 文件，打开文件夹并显示保存路径；浏览器模式继续原生下载。
2026-09-19：步骤编写页实现多插件上下文的两层折叠列表、逐条查看和删除、累积采集；API/Chat 网页生成合并为一个选择入口。
2026-09-20：网页 Chat 回复支持解析、校验、解释和程序差异预览后再采纳。API/Chat 历史可选 0、最近 1–10 轮或 -1 不限制，并可选去除重复静态字段；反馈、日志及插件上下文不压缩。本地请求上限为 128 KiB，超限不自动修改用户选择。单元回归 25 项、真实桌面全流程 1 项、工程及 diff 检查通过。
2026-09-20：运行资源改为实例池：每个任务复用一个调试实例，每条正式执行拥有独立实例并可并行。核心只负责实例路由和生命周期，浏览器等资源仍由插件 ResourceProvider 管理。设置页可配置 1–8 个执行线程，默认 8，超限时新运行立即报错。增加顶部执行实例管理、步骤级持久化插件上下文、调试错误上下文可见/可排除、Playwright locale 配置和变量优先级统一。
2026-09-21：步骤编写的五个标签页切换时统一保存草稿；进入调试时按最新任务参数、步骤参数、依赖和时间设置重建输入区。新增/修改的步骤默认值立即显示，并保留用户已手动修改的本次输入。

2026-09-19：补齐 Windows x64 便携包构建工程：PyInstaller onedir 配置、PowerShell 一键构建、Windows CI、包内 Playwright Chromium/WebView2 Fixed Runtime、发布清单和 SHA-256。当前仅完成构建能力，macOS 不交叉生成 Windows EXE；待 Windows 构建机出包及目标 Windows 10 断网 VM 验收。
2026-09-22：统一实施任务/执行/调试交互：正式执行独立为一级菜单，步骤采用自动保存、待确认与已确认状态，调试按任务复用实例、正式执行按运行隔离并支持 1–8 并发。AI 增加目标描述、内容生成及按轮次修复；插件上下文按步骤持久化并在修改时使确认失效。新增 OCR/TiDB、插件视图、任务导入导出、运行数据清理、默认环境与工作空间迁移。正式包移除 demo/sample；现有数据库直接迁移 captcha→ocr。补充工作空间迁移删除保护、按步骤隔离调试补充说明和桌面端串行保存。
2026-09-22：步骤“目标描述”统一为“步骤描述”，AI 生成入口移到字段标题右侧；新增随步骤保存、确认和导入导出的 AI 编写说明。提示词集中到 application/prompts.py，区分步骤描述整理、步骤内容生成、失败修复、能力纠错及网页渠道协议。网页 Chat 明确要求换行 `\\n`、缩进 `\\u0020`、双引号 `\\\"` 和反斜杠转义，仍由本地解析、语法校验和差异预览后采纳。编写页由五个 Tab 改为四个 Tab 加右侧调试抽屉；流程调试可携带原 run/attempt 打开真实失败步骤，“新一轮调试”改为清空 AI 修复上下文。
2026-09-22：步骤工具栏五个操作等宽铺满步骤栏；调试区改为步骤编辑主卡片内的可折叠右侧分栏，以中间箭头展开/收起，不再重复展示插件上下文。采集插件上下文可在采集前填写可选名称。环境变量新增持久化说明，任务/步骤参数沿用 JSON Schema `description`；运行和调试输入就近显示说明。控制数据库升级到 v8。
