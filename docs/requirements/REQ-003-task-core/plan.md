# REQ-003 实施计划

- [x] 核对依赖 REQ-002 的实现与契约。
- [x] 任务/步骤定义、稳定 ID、当前内容和运行记录。
- [x] 输入常量/任务参数/步骤输出引用及类型检查。
- [x] 单步、下一步、执行到选中步骤与连续执行。
- [x] 运行和尝试状态、失败停止、重试记录和下游结果过期。
- [x] 按 README 验收标准运行针对性检查并记录证据。
- [x] 更新本需求、REQ-001 总需求池和项目进度。

依赖未完成时可整理接口和原型，不将未验证的功能标为完成。

## 本地运行与存储

- [x] 核对 REQ-002 中预先确定的插件与运行契约。
- [x] SQLite 表结构、明确的 schema 版本和迁移。
- [x] 任务配置、步骤尝试、有效输入摘要及结果引用。
- [x] 协调器/worker 命令事件、单活跃会话、幂等命令。
- [x] 暂停、协作取消、失联恢复和 UNKNOWN 结果处理。
- [x] 本地数据目录、日志脱敏、环境和凭据引用。
- [x] 按 README 验收标准运行针对性检查并记录证据。
- [x] 更新本需求、REQ-001 总需求池和项目进度。

依赖未完成时可整理接口和原型，不将未验证的功能标为完成。

插件调用从第一版即经过 REQ-002 定义的动作执行接口；REQ-003 使用最小测试适配器验证，不等待 REQ-004 完整插件管理。控制流节点和迭代/嵌套执行地址也在总体设计预留，详细实现见 REQ-008。

- [x] 实现 AI 编写端口、插件上下文组装、step content 生成与修订。
- [x] 实现内容校验、试跑反馈、保存已验证内容及草稿/反馈传递。

- [x] 无模型配置时，纯手写 step content 仍可校验、试跑、固化并执行。
- [x] AI 与手动交替编辑采用同一保存与验证规则；修改后旧验证失效，AI 建议不直接覆盖手动改动。

- [x] 步骤只保存当前内容和验证状态，不创建步骤版本表。
- [x] 总库管理配置和执行记录；两个任务可有同名业务表且互不影响，同任务结果按 run_id 区分。
- [x] 无业务数据的浏览器动作只写总库状态，不创建任务库；返回数据时核心自动创建任务库并保存，后续步骤从库中读取。
- [x] 在结果已写入、总库未确认时模拟崩溃，通过 attempt_id 核对，不重复执行副作用。

- [x] 验证核心任务存储接口与插件结果处理分工：无数据不建任务库、业务表/文件正确归属任务、保存失败不盲目重跑业务。

上述核心工作已交付，验收证据与实机/真实模型限制见 validation.md。完整插件包迁移与发现由 REQ-004 实施，不属于本轮完成声明。
