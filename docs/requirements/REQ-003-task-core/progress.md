# REQ-003 进度

2026-09-17：交付可运行 Python 应用、CLI 与本地 HTTP API。已实现任务/步骤当前配置、手写与 AI 建议、内容校验、试跑确认、spawn worker 顺序执行、默认任务库输出、跨步骤引用、命令去重、暂停/取消/重试、不确定状态核对及 receipt 恢复。

使用本地 demo/text 测试适配器验证统一插件接口；实际模型服务、Playwright 和 Windows 10 x64 实机尚未验证。无 UI，未生成 Windows EXE。插件包发现与自定义表兼容迁移在 REQ-004 完成。

交付入口见 [使用指南](usage.md)，当前待用户验收。

2026-09-17：按用户确认，成功试跑/正式运行不自动关闭 worker 浏览器。连续同任务同环境试跑复用资源，正式运行分段执行保留会话，显式结束关闭，成功历史不被取消覆盖。不同任务/环境/新正式运行切换独立资源，退出仍清理；真实浏览器与完整回归验证中。

2026-09-18：按用户要求允许本地环境配置与任务参数直接保存普通密钥值，摘要和 AI 提示继续脱敏；不修改操作系统环境变量，原 env: 引用可选兼容。步骤 inputs 自动合并环境变量、任务默认/运行参数与显式绑定/输入；显式值优先，封闭 schema 过滤未声明变量。任务配置保存独立于步骤表单，保留未保存步骤；空白参数行忽略、重名提示具体名称。AI 提示提供变量名称/来源/类型，不发送默认值；超长初始请求仅携带最近两轮历史，重复失败快照改为引用，最新上下文保留；仍超限明确提示减少采集内容。新增变量 3 项和网页/超长历史 3 项通过。

2026-09-18：工作台新增分阶段人工输入，核心支持必填项等待、按步骤保存本次输入及幂等提交；暂停前不产生失败尝试，恢复沿用运行及资源。API 新参数默认保持旧调用语义，无 DDL 变更。

2026-09-23：AI 编写请求的变量目录改由核心按运行时优先级合并，同名变量只保留生效定义；不向 AI 暴露变量来源或覆盖规则。描述生成和内容生成共用这份目录，TiDB 编写规则也不再描述变量来源。针对同名变量执行了单项回归和工程检查，未运行全量测试。
# 2026-09-23 AI 计划与请求契约

- AI 请求统一使用最终变量目录、purpose 场景契约和可配置 UTF-8 字节预算。
- 新增独立计划、计划上下文与生成记录；计划采集不创建任务运行记录。
- 任务包升级为 `taskweave-task-2`。AI 导入步骤全部待确认；任务导出/导入逐步保留合法确认状态。
