# REQ-006 验证计划

状态：未执行，以下为待验收场景。

1. 导出后在另一独立数据目录导入，任务步骤/参数绑定一致。
2. 缺少插件或版本不匹配时明确阻止执行并列出解决项。
3. 模板包不包含账号、Token、浏览器状态、运行 DB 和绝对本机路径。
4. 压缩包越界路径或损坏资源拒绝导入，不覆盖原任务。
5. 匹配 Windows/Python 平台的离线依赖包可在无公网环境安装验证。

## 实施时记录

每项记录环境/版本、前置数据、实际命令或操作、预期、实际结果、证据路径和限制。不得把文档检查通过当作功能验收通过。

本需求先验证基础顺序任务分享；后续控制流和图形定义的格式兼容、版本升级与导入导出回归分别由 REQ-008/009 完成，不构成本需求的前置依赖。
