# REQ-006 进度

2026-09-15：完成需求拆分与实施输入，运行功能尚未开始。

下一步：按 plan.md 核对依赖并实施。当前没有测试通过证据。

本需求先验证基础顺序任务分享；后续控制流和图形定义的格式兼容、版本升级与导入导出回归分别由 REQ-008/009 完成，不构成本需求的前置依赖。
# 2026-09-23 taskweave-task-2

- 顶层增加 `origin`，每个步骤携带 `validation_state`。
- `ai_generated` 只能导入为 `DRAFT`；`task_export` 逐步恢复导出时的合法状态。
- v1 文件不做隐式兼容，提示使用当前版本重新导出。
