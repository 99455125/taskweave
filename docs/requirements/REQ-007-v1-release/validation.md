# REQ-007 验证计划

状态：未执行，以下为待验收场景。

1. 新环境解压便携包 → 导入示例 → 选择已验证步骤 → 浏览器创建 → 人工检查 → 提交 → 查看结果。
2. 同一模板重复运行互不污染，故障重启和重复点击通过回归。
3. 第二台 Windows 使用自己的角色配置完成同一流程。
4. 备份恢复后任务及历史可读；升级迁移有回滚路径。
5. 发布记录列明实际平台/版本/命令和结果；未执行的验收保持未完成。

## 实施时记录

每项记录环境/版本、前置数据、实际命令或操作、预期、实际结果、证据路径和限制。不得把文档检查通过当作功能验收通过。

本需求交付基础顺序任务版本；条件/循环/嵌套与图形编排随后由 REQ-008/009 实现和验证。

- [ ] 提供含 Python、Playwright driver 与 Chromium 的便携包，UI 运行时随目录分发；不安装任何系统依赖。
- [ ] 在无 Python/Playwright/浏览器缓存、断网且禁止安装的目标 Windows VM，验证解压运行、窗口及固定步骤。

[打包方案](windows-package.md)

## 2026-09-19 构建工程检查

- Windows 冻结入口与运行时启动器通过 Python 语法检查。
- 冻结清单所需的 8 个 distribution（核心、四个插件、NiceGUI、pywebview、Playwright）均能从锁定环境解析。
- 工程结构、源码语法、依赖边界与 Markdown 检查通过，`git diff --check` 通过。
- macOS 上未生成 Windows EXE；PowerShell 真实构建、WebView2/Chromium 随包结果及目标 Windows 10 x64 断网 VM 仍为待验收项，不将静态检查记为便携包交付通过。
- 2026-09-20：Windows 用户实际执行暴露 PowerShell 5.1 不存在 `$IsWindows` 的兼容问题，已改为检查 `$env:OS`；同步审查并补齐 PowerShell 5.1 的原生命令退出码处理和各关键产物实体校验。Windows 实机需从该检查后继续构建并记录下一个实际结果。
