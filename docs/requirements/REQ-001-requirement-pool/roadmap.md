# 实施路线

REQ-002 → REQ-003 → REQ-004 → REQ-005 → REQ-006 → REQ-007 → REQ-008 → REQ-009。

1. 总体设计先定义 step content、AI 编写端口、插件执行与编写贡献协议。
2. 任务与步骤实现 AI/手动两种编写方式，共用调试、固化、执行和存储。
3. 插件需求实现统一机制，并通过真实 Playwright 插件及最小示例插件验证，避免核心硬编码浏览器能力。
4. 基础操作台接入以上服务；插件可先通过应用服务/测试入口验证，无需依赖 UI 完成。
5. 完成任务分享、Windows 基础版本交付。
6. 最后增加控制流和拖拽编排，并验证分享、持久化和旧版本兼容。

REQ-007 交付基础版本；全部计划完成还需 REQ-008/009 验收。Windows、离线安装及真实业务必须实际验证。当前仅完成规划。
