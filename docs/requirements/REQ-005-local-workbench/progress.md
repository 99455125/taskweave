# REQ-005 进度

2026-09-15：完成需求拆分与实施输入，运行功能尚未开始。

下一步：按 plan.md 核对依赖并实施。当前没有测试通过证据。

2026-09-17：用户确认客户端扩展为 Windows / macOS 跨平台独立窗口，共用 NiceGUI 界面和应用服务。已更新设计与分平台验证边界；UI 尚未实现，Windows 免安装约束继续有效。

2026-09-17：交互原型调整为任务内二级导航，新增上一步成功后的执行间隔设置与等待状态。仅完成原型和设计修改；等待用户确认，尚未实施客户端或间隔调度。

2026-09-17：确认原型后完成可选 desktop 包和 workbench 命令，任务内导航、参数表单、动作插入、AI 差异采纳、试跑确认、运行控制/历史、结果预览、环境与插件配置已接入真实服务。总库升级 v3，新增执行间隔、等待截止时间与定义快照。macOS 原生窗口已启动并回传 1280×850 窗口尺寸，退出成功。完整 UI 验收初次通过；最终回归证据记于 validation.md。Windows、Apple Silicon 与冻结包不声明已验收。

2026-09-17：断电后恢复完整验证，52 项测试全部通过（269.131 秒），包含核心、Playwright、间隔恢复和真实界面操作。工程校验、8 项设计检查、core 与 Playwright wheel 构建均通过。REQ-005 源码客户端交付，待用户验收；Windows / Apple Silicon 实机与冻结包仍未验证。

2026-09-17：修复默认启动端口检测。当前机器 localhost IPv4 bind 实测报 Errno 49，而 127.0.0.1 正常；取消 NiceGUI 的 localhost 固定范围探测，改由系统分配 IPv4 回环端口。REQ-005 12 项针对性检查通过（25.222 秒）；未指定 --port 的 macOS 原生窗口实际启动，回传 native_window_ready=true、size=[1280,850]。此前 52 项完整回归记录保持为修复前证据。

2026-09-17：改善模型连接错误提示，区分 HTTP 状态、网络/代理、TLS 证书、超时及响应格式；不展示服务端原文或密钥。模拟 401、400 和网络错误检查通过，真实账户请求未验证。

2026-09-17：实际复现 uv Python 默认 CA 文件缺失导致 DeepSeek TLS 失败。增加显式 certifi 依赖，模型请求采用完整公开 CA，仍校验证书及域名；SSL_CERT_FILE/SSL_CERT_DIR 可补充自定义 CA。无密钥实际 HTTPS 请求到达 DeepSeek 并返回预期 HTTP 401，未使用真实账号请求。

2026-09-17：补齐 AI 服务适配与独立实时日志窗口。DeepSeek 使用非思考请求参数；解析兼容 nullable tool_calls、JSON 围栏，拒绝空/截断/缺失步骤内容，连接测试检查 async run 语法。默认模型保持 Chat Completions，未新增 Responses/Anthropic 协议。

顶部服务日志按钮在 native 模式打开独立 pywebview 子进程，浏览器检查模式新开标签；日志经 NiceGUI Socket.IO 推送，支持筛选、暂停和下载，保存在 home/logs/server.log。记录 Python logging 与服务操作阶段，worker 业务日志继续在任务页面；不宣称已收集全部 stdout/stderr。服务退出回收日志窗口。

3 项模型/日志检查通过；12 项 REQ-005 回归通过（28.127 秒）；真实界面验收通过（96.504 秒），包含 nullable tool_calls、日志新窗口与新增 AI 请求实时追加。macOS native 主窗口启动、日志按钮拉起独立窗口子进程并随服务退出回收已验证。真实用户密钥仍在用户进程会话内，未验证 DeepSeek 账户生成；此前无密钥 HTTPS 检查只证明 TLS 正常。

2026-09-17：针对实际日志中偶发 JSONDecodeError，拆分接口响应与模型内容解析，记录阶段、行列及长度，不记录正文；无效或空模型 JSON 最多自动重新生成一次，接口非 JSON 不作为模型错误重试。连接测试增加合法 JSON 示例。5 项模型/日志检查通过，覆盖失败后修正成功、二次失败停止和代理 HTML 响应。真实账号偶发失败仍需重启后的新日志定位，不能据截图断定其根因。

2026-09-17：用户要求简化 API Key 保存，现与其他模型配置一起写入本地 model.json，启动自动读取；留空保留同地址密钥，更换地址不沿用。持久化/恢复/切换地址针对性检查通过（0.364 秒），其余 11 项 REQ-005 检查通过；同步更新界面与当前指南，日志继续脱敏。

2026-09-17：步骤编写改为选择插件，保存时展开为选中插件全部当前动作；既有能力列表显示为插件选择，缺失插件保留不可用提示。动作插入自动选择所属插件，插件管理可展开动作说明及输入 schema。AI 生成若引用未授权动作，反馈精确动作清单并重新生成一次；仍失败显示具体动作，不绕过执行权限。6 项模型/编写/日志检查通过，真实界面回归待记录。

2026-09-17：按用户确认，成功试跑/正式运行不自动关闭 worker 浏览器。连续同任务同环境试跑复用资源，正式运行分段执行保留会话，显式结束关闭，成功历史不被取消覆盖。不同任务/环境/新正式运行切换独立资源，退出仍清理；真实浏览器与完整回归验证中。

2026-09-17：真实 Chromium 新用例通过（13.491 秒），验证同任务五步试跑共用 PID/页面、正式运行到第三步后继续到第五步只产生五次尝试、成功后仍保留进程、显式结束后资源释放且历史保持成功。完整 60 项检查（255.042 秒）中 58 项通过，包括真实 UI；两项测试在启动后修订导致运行进程仍使用旧测试内容：新增用例首步骤无业务输出，及旧模型桩少一次修正响应。修订后分别单独通过（13.491 秒、0.357 秒），实现未因此更改。日志在 .runtime/retained-browser-validation.log，不能将该轮日志标为完整通过。工程与 diff 检查通过。

2026-09-17：修复 DesktopController.call 的 operation 形参与 run.control 参数冲突，原按钮 TypeError 导致没有发出关闭命令。调用名称改为 api_operation。试跑日志旁新增“AI 根据试跑日志修复”：携带最近尝试错误及脱敏日志（最多 50 条/16KB），共用差异预览和采纳，不自动执行或固化。普通生成按钮也包含已有试跑反馈和日志。真实浏览器失败关闭与界面回归验证中。

2026-09-17：真实浏览器超时失败后，经 DesktopController 关闭资源的检查通过（9.869 秒）；反馈包含最近尝试错误及日志。真实界面回归通过（91.967 秒）。按用户追加要求，AI 请求对话及回复正文打印到服务日志，仍不记录 Authorization 头，API Key 脱敏；7 项模型/编写/日志检查通过（1.475 秒），覆盖对话可见和认证头不泄露。该记录取代此前不记录模型正文的约定。

2026-09-17：修复重启执行器残留锁：持有协调器 OS 锁后清除旧 runtime_lease，正常退出也释放占用；历史运行与 UNKNOWN 尝试保留，不自动判定副作用。步骤反馈重开时从 DB 恢复最新试跑，重新显示结束入口。新试跑创建前检查占用，同任务同环境已知安全失败允许重新试跑，未知副作用必须先结束/核对，不新增被阻塞的孤立试跑记录。12 项 REQ-005 回归通过（18.068 秒），新增死 owner 残留锁重启检查通过（1.574 秒），结果提交恢复检查通过（1.803 秒）；工程及 diff 检查通过。

2026-09-17：按用户要求补齐当前试跑轮次 AI 会话内存历史，按步骤隔离，真正启动新的试跑时清空，后续修复请求携带本轮对话。日志修复先弹出补充说明框；采纳后立即保存 DRAFT 并刷新当前页保留原标签，下一次试跑使用最新草稿。ctx.call 的关键字 action_id 与位置参数采用同样权限检查，动态动作名不允许。8 项模型/历史/权限/日志检查通过（1.803 秒），旧界面全流程通过（97.126 秒）；新增补充说明及落库界面检查正在执行。

2026-09-17：新增补充说明、修复建议采纳落库的真实界面检查最终通过（127.284 秒）；验证补充说明进入模型请求、采纳源码存入 steps 且状态为 DRAFT、日志实时追加。9 项模型/历史/权限/日志/最新草稿执行检查通过（3.048 秒）。首次新增界面检查最后因日志成功记录不再唯一而失败，调整测试定位器后通过；实现未因此更改。工程与 diff 检查通过。

2026-09-18：用户五项调整已实施：长行日志换行；正式执行横向列表、新建执行选择环境、步骤点击三选项以及仅清理本次结果从头执行；环境名称+Key/Value、角色/凭据统一变量；Playwright 从任务参数读取并提供配置变量说明；代码差异由程序生成；增加手动确认成功保存和验证来源。总库 v4 备份迁移。新增 4 项核心/重启/环境/参数检查通过（4.193 秒），v2→v4 迁移检查通过（0.850 秒）。完整回归执行中。

2026-09-18：五项调整完整回归通过：69 项（282.904 秒），含真实 NiceGUI/Chromium 界面、新建执行及点击步骤继续、通用环境编辑、手动确认与本次执行重置隔离、Playwright 任务参数优先级。工程检查与 REQ-002 的 8 项设计检查通过，git diff --check 通过。日志见 .runtime/workbench-sept18-validation.log。未进行 Windows 便携包验证。

2026-09-18：正式执行按钮统一为“结束执行”，试跑为“结束调试”。点击先确认，按执行定义快照与插件资源声明展示将释放的资源；Playwright 提供可选 resource_descriptions 说明浏览器/页面/上下文，其他插件无说明则显示资源 ID。仅关闭已创建资源，无资源则提示释放执行器。取消不发送控制命令，保存结果与历史保留。工程检查通过，4 项相关运行/重置/配置回归通过（3.647 秒）；本轮未重跑完整 UI 集成。

2026-09-18：日志窗口删除浏览器式下载入口，改为打开本地日志目录；新增复制当前显示日志（macOS pbcopy / Windows clip，Linux 浏览器剪贴板），清空与筛选同步维护显示缓冲，并开放文本选择。日志始终自动保存到 logs/server.log。工程检查与 9 项模型/日志回归通过（3.584 秒）；未验证 Windows 系统剪贴板或重跑原生窗口。

2026-09-18：按用户简化要求，移除“复制当前日志”按钮及系统剪贴板调用，仅保留“打开日志目录”；清空、级别筛选、暂停和可选中文字仍保留。

2026-09-18：普通 AI 生成/修订不携带或写入调试历史，也不自动附带试跑反馈；日志修复显式 use_history=True，累计当前调试过程的修复对话。反馈页再次执行试跑沿用上一试跑环境和实际输入、保存并执行最新草稿，不弹环境确认且不清空历史。内容页发起新的试跑仍重置调试历史，内存按步骤隔离。新增 use_history/continue_session 均可选，默认无历史/新试跑，兼容旧接口。9 项相关模型/历史/草稿回归通过，工程检查通过。

2026-09-18：添加步骤默认继承任务末尾步骤的全部插件，并展开当前插件完整能力；先保存当前编辑再读取最新步骤，避免丢失刚调整的插件。首步骤仍默认无插件，内容/输入/验证状态不复制，新步骤可自行调整插件。

2026-09-18：上下文观察支持成功试跑/正式执行的保留会话，成功验证保存后可供后续步骤采集。新增 run.context.sessions 仅返回本进程实际存活、当前 task 匹配且未执行中的保留会话；重启自动为空，不把 DB 历史作为会话。采集接口允许 SUCCEEDED 无租约，但强制校验 session_run_id，避免旧运行误读新浏览器。

2026-09-18：修复失败调试阻挡修订：仅步骤保存允许空闲 FAILED/SUCCEEDED TRIAL 租约，不释放浏览器，不放开正式运行或执行中编辑锁。反馈页显式重试允许同任务环境失败调试租约移交到新尝试，保留旧 UNKNOWN/FAILED 证据；效果未知时提示用户检查页面并确认再次执行，不强制结束浏览器，记录 USER_RETRY_WITH_RETAINED_SESSION。真实 Chromium 超时失败→最新草稿修订→同 PID/页面再试跑成功→显式结束检查通过（11.588 秒）。工程检查通过。

2026-09-18：试跑反馈页新增“从首步开始”：确认环境/任务输入后关闭当前同任务调试资源，新建 flow_trial（TRIAL 的可选 request_json 标志，无 DDL 变更），执行第一步到当前步骤，使用正式绑定及数据库前序结果和间隔，失败停止，不要求预先成功验证；历史试跑保留。普通反馈页继续试跑仍保留页面。6 项相关回归通过（6.918 秒），包含草稿前序依赖及全新 worker 检查。

2026-09-18：按确认方案实施 Playwright 四项优化：可见控件快照/可靠定位与状态、输入值及 URL/标题/文字结果等待、动作开始/失败/耗时与失败快照、修复前自动刷新保留会话、插件专属提示词。升级插件到 0.2.0、API v1 不变、无 DDL 变更。新增真实 Chromium 连续搜索检查通过（11.097 秒）：前步打开页面→隐藏旧控件失败→新快照进入模型请求→修订使用可见控件→同 PID 输入点击及实际结果断言成功，再次采集为新时间及结果页面。原人工提交场景单独回归通过（31.552 秒）。完整回归执行中。

2026-09-18：Playwright 四项优化完整回归 72 项通过（339.693 秒），包含真实 NiceGUI/Chromium 界面、插件、运行/存储。运行中追加的历史快照引用优化另以模型/历史 10 项通过（6.001 秒），最终快照与无租约 READ 失败继续采集另以真实连续搜索用例通过（9.481 秒）。工程检查、设计 8 项检查及 diff 检查通过。浏览器搜索使用本机测试站点，AI 使用模型 fixture，未声称真实 DeepSeek/百度端到端成功。首次插件回归发现普通元素不可调用 is_editable、LOCATOR 诊断范围及旧测试误把 selector.value 当成业务输入值；修正后完整回归通过。

2026-09-18：结束按钮改为 run.get 的实时 can_end（当前存活 worker/session 或当前运行租约），不按 SUCCEEDED 历史状态推断。显式结束后的成功历史、未启动执行和重启后历史不显示按钮；保留会话或占用执行器时显示，试跑结束入口同规则。核对结果仍需选择已完成/未完成，说明改为选填，后台允许空 evidence，不取消人工核对。7 项相关检查通过（22.133 秒），未知执行人工核对空说明后继续检查通过（2.637 秒）；工程与 diff 检查通过。无数据库迁移，can_end 是实时派生字段。

2026-09-18：运行历史、试跑反馈和运行日志改为步骤序号及执行时名称；执行列表与上下文会话使用执行类型、时间、环境及状态，不展示内部 UUID。历史优先使用 definition_json 快照，旧记录回退当前步骤，无法解析显示未知步骤。仅转换界面基础设施元数据，业务结果、存储、接口与服务日志不变，无数据库迁移。

2026-09-18：新增 AI Chat 生成/修订与 AI Chat 根据历史修订入口，无模型配置亦可导出项目规范、插件能力、上下文/最新日志/可选调试对话及补充说明。粘贴网页回复 JSON 或 Python 代码，采纳保存草稿并重绘当前页，保留反馈页继续试跑入口及环境。历史修订采纳回合仅保存内存；普通导出不带历史、不增加调试对话。插件 AuthoringContribution 新增可选 channel_overrides（api/web_chat，instructions/examples），默认空保持现有插件兼容；Playwright 提供网页专属规则，禁止假装调用本机工具。无 DDL 变更。

2026-09-18：运行历史名称与网页 AI 编写交付：显示检查 3 项通过；网页导出/回复 2 项、模型/历史 10 项、插件契约 5 项通过；真实 NiceGUI/Chromium 全流程（含网页导出、粘贴采纳、无额外模型请求及历史补充说明）通过，114.968 秒。变量调整后同一界面全流程再次通过，107.419 秒。

2026-09-18：按用户要求允许本地环境配置与任务参数直接保存普通密钥值，摘要和 AI 提示继续脱敏；不修改操作系统环境变量，原 env: 引用可选兼容。步骤 inputs 自动合并环境变量、任务默认/运行参数与显式绑定/输入；显式值优先，封闭 schema 过滤未声明变量。任务配置保存独立于步骤表单，保留未保存步骤；空白参数行忽略、重名提示具体名称。AI 提示提供变量名称/来源/类型，不发送默认值；超长初始请求仅携带最近两轮历史，重复失败快照改为引用，最新上下文保留；仍超限明确提示减少采集内容。新增变量 3 项和网页/超长历史 3 项通过。

试跑、新建执行及从首步试跑的表单自动带出所选环境与任务默认值；切换环境更新默认值、保留手动修改项。任务必填参数无默认值时可从环境读取，空白参数行不阻挡保存。相关检查与实际界面回归执行中。

2026-09-18 最终交付：本地变量和表单自动预填、独立保存任务配置、网页 AI 两个入口及回复采纳、插件渠道适配、历史名称、超长历史保留最近两轮、本地 captcha 插件与通用元素采图均已实施。变量 5 项（1.798 秒）、执行回归 7 项（6.324 秒）、网页/中文/超长历史 4 项（1.363 秒）通过；核心 26 项、模型 10 项、显示 3 项、插件契约 5 项、OCR 3 项、连续搜索 1 项及真实界面 1 项检查通过。工程检查覆盖 101 个 Markdown 文档并通过，diff 检查通过。实际 OCR 与浏览器登录使用本机测试图片/站点；未操作用户真实登录页，未验收 Windows 便携包。无 DDL 变更；Playwright 0.3.0、captcha 0.1.0、插件 API v1。

### 2026-09-18：任务配置再次打开显示最新值

每次打开已有任务的配置窗口，按任务 ID 重新读取配置，避免按钮捕获的旧数据使名称、说明、参数回退。保存配置继续保留当前步骤编辑内容，无需返回任务列表刷新。

### 2026-09-18：试跑变量来源与列表布局

修正重复试跑忽略当前环境和任务输入的问题。新增三组可折叠变量展示，环境及步骤只读、本次任务输入可编辑。步骤四个操作按钮与纵向滚动列表分离；执行步骤横向滚动、右侧操作固定，状态改为颜色和图标。

2026-09-18：完成绿底白字、勾位于标题后的步骤与执行状态样式；依赖表单按来源隐藏无关字段，成功结果字段可下拉选取，保存绑定自动声明输入。反馈页执行与从首步试跑复用上方环境/输入，任务及未绑定步骤变量可填写本次值，必填项按任务/步骤阶段暂停收集。


### 执行重跑与参数补录（2026-09-18）

- 删除任务入口位于任务列表，确认后删除；任务配置只负责编辑和保存。
- 步骤验证不要求与执行环境匹配，修改环境不使步骤重新变成草稿。环境提供变量，不作为验证通过的条件；已创建执行仍保留配置快照一致性检查。
- 点击已成功步骤，可选择开始步骤，执行到点击的步骤。清理开始步骤及其后续输出、尝试和步骤本次输入，前面的成功结果保留，其他执行不受影响。中间重跑复用当前运行资源；从头重跑清理全部结果并重新建立资源。
- 每次执行的详情在该执行行下方展开或收起，步骤横向滚动，操作按钮固定在右侧。
- 缺少任务或步骤必录参数时，在对应执行点暂停并弹窗补录。调试反馈继续展示环境、任务和步骤三类变量，必录字段标注“必录”。取消补录保持暂停，可再点击补录按钮继续。
- `run.restart` 新增可选 `start_step_id`，旧调用仍从头执行，无数据库迁移。选择中间起点时，其之前的步骤必须已成功，避免悄悄执行未选择的前置步骤。


从首步试跑后，AI 修复使用该次流程试跑中当前步骤的失败信息及日志。如果失败在前面的步骤，明确提示其名称并要求切到该步骤修复，避免将前置步骤的错误用于改写当前步骤。


### 执行卡片清理与一致的补录界面（2026-09-18）

每次执行卡片右上角提供 × 删除入口，确认后仅清理该次执行、日志和结果，其他执行与任务保留。正在运行的步骤须结束后删除，保留资源的完成运行删除时释放资源。增加 `run.delete` 接口，无 DDL 变更。删除执行列表外的独立详情容器。仅在选中的执行内部创建详情；收起或切换执行时删除原容器，不保留隐藏面板。

执行试跑、从首步开始、结束测试统一放在反馈顶部的一列；状态成功绿、失败红、普通灰，不在每次尝试下重复结束按钮。全局普通按钮采用中性色，移除蓝色底色。

独立试跑弹窗采用自身选中的环境和对应变量；开始后同步反馈页的环境及本次输入，后续试跑使用这次选择，避免重新使用页面旧环境。任务/步骤分组默认仅展开有必录输入的部分。执行补录弹窗复用同一分组界面，固定显示本次执行环境、任务变量与当前步骤变量/依赖，依赖结果读取本次运行的 DB；默认展开当前需要补录的分组，其他分组只读。


上述交互经真实界面完整复验通过（77.137 秒）。处理中临时禁用相关操作按钮，避免连续点击被忽略；处理后恢复原有效状态，结束后的结束测试按钮保持禁用。


### 默认环境和首步补录优化（2026-09-18）

环境列表可设为默认，默认项左上角显示“默认”；默认环境 ID 存入本地 workbench.json，重启保留并作为首次环境选择。任务列表删除按钮放在最右侧。普通按钮白底蓝字；优先/选中操作浅青底、青绿边框，不使用蓝色底。执行删除 × 红色，与其他操作留出间距。

执行详情在所属执行内部展开，头部提供“收起详情”，执行行的详情/收起开关仍保留。补录弹窗放在稳定页面容器下，每个 InputRequested 生成唯一请求 ID，同一请求刷新不重建、不重复弹出，稍后填写可手动重开。任务参数和首步可编辑参数在首次补录中共同输入，提交时原子保存任务输入及首步 step_inputs；run.inputs 新增可选 step_inputs，旧调用保持兼容，无 DDL 变更。后续步骤到达时仍按需补录。

### 2026-09-18 输入与操作布局统一

- 运行输入显示已有值或默认值；未配置时输入框保持空白，灰色提示“未配置默认值，请录入”，不再重复显示未配置的步骤默认值说明。
- 试跑三个操作横排，统一白底描边；成功使用浅青色，失败使用浅红色，与其他操作按钮保持一致。
- 对话框及页面中的相邻操作按钮横排，在窄窗口自动换行。

- 步骤确认合并为一个“确认验证并保存”按钮：当前内容有成功试跑证据时直接确认；没有有效证据时弹窗询问是否手动验证通过，取消则保留草稿。

- 试跑反馈的 API 与 Chat 修复入口合并为上方“AI 修复”，与三个运行操作横排；点击后选择 API 或 Chat 网页修复，沿用日志及补充说明流程。

- 2026-09-19：结果展示声明 v1、插件 result_views 扩展、Playwright 截图展示、执行列表直接查看和详情状态图标已实现；验证记录见 REQ-005。

### 2026-09-19 TiDB 与任务配置分享

- 可选 database extra 提供 `plugins/tidb`，使用 PyMySQL/MySQL 协议，连接配置读取环境及任务变量，提供连接测试、表结构采集、参数化只读查询和 tidb.table/tidb.verification 展示器。数据库连接每次关闭；API/Chat 共用插件格式，Chat 不假装可以访问数据库。
- 任务列表提供导出 JSON、复制 JSON、下载 JSON，以及粘贴导入为新任务。`taskweave-task-1` 包含任务名称、说明、参数默认值、按序步骤内容/参数/插件要求/绑定；前序依赖通过可移植步骤 key 重映射。环境、历史、文件和执行结果不导出。导入执行静态检查和插件校验，不运行步骤，全部标记 MANUAL/VALIDATED；缺失插件或无效配置拒绝导入，失败不留下半个任务。只支持当前线性步骤配置。
- 环境列表支持确认后删除，保留历史业务结果，历史执行保存 deleted_environment_name 并解除外键；保留资源、运行或暂停中的环境禁止删除。默认环境删除后清除默认设置。环境不提供导入导出。


### 2026-09-19 任务一键清理

任务列表新增「清理运行」，确认后保留任务配置、参数、步骤与验证状态，删除该任务全部正式执行/试跑记录、结果引用、事件和命令记录、任务结果数据库、截图文件、staging 与插件迁移备份，并清理调试对话。保留会话关闭；正在执行步骤时拒绝操作，防止写入冲突。其他任务和环境不受影响。应用公共 server.log 是服务日志，不属于单个任务运行数据，仍保留。新增 task.clear_runs 接口，无 DDL 变更。清理后可重新创建执行。


2026-09-19：结果窗口仅按步骤 views 声明生成展示页签，删除自动文件页签逻辑。保存但未声明展示的文件不自动展示；原始 JSON 保留。不改动结果存储或已有步骤。


2026-09-19：修复任务导出复制和桌面下载。macOS 桌面通过系统剪贴板写入 UTF-8，成功后提示；浏览器复制等待实际操作结果，失败显示错误。网页 AI 对话复制共用此路径。桌面「下载 JSON」保存到应用数据目录 exports 下独立 JSON 文件，打开文件夹并显示保存路径；浏览器模式继续原生下载。
## 2026-09-19 多插件上下文与生成入口

- “已采集的上下文”采用外层整体折叠、内层逐条折叠的结构。
- 采集结果按条累积，展示提供者、来源和采集时间；每条可独立删除。
- 多个插件及同一插件的多次采集可共同进入下一次 API 或 Chat 网页生成。
- API 与 Chat 网页生成合并到一个“AI 生成 / 修订”入口后选择方式。
- 2026-09-20：网页 Chat 回复改为“解析并预览—查看解释和程序差异—采纳到编辑器”；提示词要求严格 `step_content`/`explanation` JSON，同时兼容网页附加文字。API 与 Chat 均可选历史 `0`、最近 `1–10` 轮或 `-1` 不限制，并可选择是否去除历史中的重复静态字段。本地请求上限调整为 128 KiB，能力目录、页面上下文和日志不压缩，超限不自动改变轮数。相关单元测试 25 项及真实桌面全流程 1 项通过。
- 插件采集上下文按步骤持久化到数据库；步骤详情和调试页共用同一组记录，并标注采集来源、支持改名和逐条删除。

2026-09-20：步骤编写标签统一为“步骤详情 / 动作表单 / 输入依赖 / 时间设置 / 调试”。进入调试会新建 AI 调试轮次，但复用任务的调试实例和插件资源；本轮 AI 对话可连续，页面不提供历史轮次选择。实际执行代码、失败动作、快照和日志在发给 AI 前全部可见，可逐项排除。

2026-09-20：顶部增加“执行实例”入口。调试按任务复用一个实例，正式执行按运行隔离实例，不同正式执行可并行；插件 ResourceProvider 继续完全负责各自资源的创建、复用和关闭。

2026-09-20：网页 Chat 回复约束明确为严格 JSON，`step_content` 直接携带完整步骤代码，`explanation` 携带说明。粘贴器可从前后带网页建议文字的整段回复中提取第一个有效步骤 JSON，仍兼容纯 Python 代码；采纳前校验语法、单一异步入口、能力权限及插件 lint，错误内容不落草稿。
