# REQ-005 验证记录

环境：macOS x64、uv Python 3.13、NiceGUI 3.17.0、pywebview 6.2.1、Playwright 1.58.0 与匹配的完整 Chromium。

## 实际验证

- 9 项间隔与应用集成检查初次通过（27.812 秒）；后续补充跨 Application 重启的等待截止时间检查，最终完整回归结果见下方。
- 实际 NiceGUI 服务 + 独立 Chromium + 本机 HTTP 模型 fixture 的界面全流程通过（102.703 秒）：任务创建、两种示例动作参数表单、AI 差异采纳、调试确认、步骤间隔设置、固定任务执行、历史快照、插件及环境配置。模型只调用一次，正式执行未再次调用；UI 资源没有外部 CDN 请求。
- 默认无启动凭据的页面请求被拒绝；跨来源检查由 LocalAccess 同时用于 HTTP / Socket.IO。
- macOS 独立客户端实际启动，原生窗口回传 native_window_ready=true、size=[1280,850]、platform=darwin；Ctrl+C 退出成功。没有把浏览器检查模式当成原生窗口验收。
- 回归发现新等待检查改变了立即点击暂停的既有语义；已修正为首步骤完成后暂停，相关旧回归单项通过（9.355 秒）。

## 命令

```bash
uv run --extra gui --extra browser --extra sample python -m unittest discover -s tests -v
uv run --extra gui --extra browser --extra sample taskweave --home .runtime/req005-native-test workbench --port 8789
uv run --extra gui --extra browser --extra sample python scripts/check_project.py
uv run --extra gui --extra browser --extra sample python scripts/check_req002.py
```

## 剩余验证边界

Windows 10 x64、Apple Silicon、真实外部模型账号和真实内网未提供，不声明这些环境已验收。REQ-007 的 EXE / app 冻结包、Windows 禁安装和断网条件不由源码运行检查替代。复杂 JSON 参数允许使用文本编辑；基本任务及动作无需编辑 JSON。

## 最终回归 · 2026-09-17

断电后重新执行完整测试，52 项全部通过，耗时 269.131 秒：REQ-003 核心 26 项、REQ-004 插件 14 项、REQ-005 调度/应用 11 项、真实界面集成 1 项。日志位于本地 `.runtime/req005-final-validation.log`。

界面验收进一步覆盖 AI 内容手动修改、调试固化、会话 API Key 不落盘，以及正式执行不调用模型。截图位于 `.runtime/req005-ui-evidence/`。

回归同时修正 Playwright 失败截图继承过短业务超时的问题，诊断截图独立使用 3000ms 超时；浏览器测试通用动作超时调整为 5000ms，仅预期失败的动作使用 700ms。提交后进程丢失及失败截图检查均在完整回归中通过。

工程检查通过（100 个 Markdown、语法、依赖边界与 CLI）；REQ-002 设计检查 8 项通过；git diff --check 通过。最新 core 和 Playwright wheel 构建成功，产物位于 `.runtime/req005-dist/`；wheel 不代表已完成 EXE/app 打包。

2026-09-17：修复默认启动端口检测。当前机器 localhost IPv4 bind 实测报 Errno 49，而 127.0.0.1 正常；取消 NiceGUI 的 localhost 固定范围探测，改由系统分配 IPv4 回环端口。REQ-005 12 项针对性检查通过（25.222 秒）；未指定 --port 的 macOS 原生窗口实际启动，回传 native_window_ready=true、size=[1280,850]。此前 52 项完整回归记录保持为修复前证据。

2026-09-17：改善模型连接错误提示，区分 HTTP 状态、网络/代理、TLS 证书、超时及响应格式；不展示服务端原文或密钥。模拟 401、400 和网络错误检查通过，真实账户请求未验证。

2026-09-17：实际复现 uv Python 默认 CA 文件缺失导致 DeepSeek TLS 失败。增加显式 certifi 依赖，模型请求采用完整公开 CA，仍校验证书及域名；SSL_CERT_FILE/SSL_CERT_DIR 可补充自定义 CA。无密钥实际 HTTPS 请求到达 DeepSeek 并返回预期 HTTP 401，未使用真实账号请求。

证书修复后真实界面全流程回归通过（1 项，91.318 秒），包含本地模型连接、生成与固定执行。工程检查及 diff 检查通过。

2026-09-17：补齐 AI 服务适配与独立实时日志窗口。DeepSeek 使用非思考请求参数；解析兼容 nullable tool_calls、JSON 围栏，拒绝空/截断/缺失步骤内容，连接测试检查 async run 语法。默认模型保持 Chat Completions，未新增 Responses/Anthropic 协议。

顶部服务日志按钮在 native 模式打开独立 pywebview 子进程，浏览器检查模式新开标签；日志经 NiceGUI Socket.IO 推送，支持筛选、暂停和下载，保存在 home/logs/server.log。记录 Python logging 与服务操作阶段，worker 业务日志继续在任务页面；不宣称已收集全部 stdout/stderr。服务退出回收日志窗口。

3 项模型/日志检查通过；12 项 REQ-005 回归通过（28.127 秒）；真实界面验收通过（96.504 秒），包含 nullable tool_calls、日志新窗口与新增 AI 请求实时追加。macOS native 主窗口启动、日志按钮拉起独立窗口子进程并随服务退出回收已验证。真实用户密钥仍在用户进程会话内，未验证 DeepSeek 账户生成；此前无密钥 HTTPS 检查只证明 TLS 正常。

2026-09-17：针对实际日志中偶发 JSONDecodeError，拆分接口响应与模型内容解析，记录阶段、行列及长度，不记录正文；无效或空模型 JSON 最多自动重新生成一次，接口非 JSON 不作为模型错误重试。连接测试增加合法 JSON 示例。5 项模型/日志检查通过，覆盖失败后修正成功、二次失败停止和代理 HTML 响应。真实账号偶发失败仍需重启后的新日志定位，不能据截图断定其根因。

2026-09-17：用户要求简化 API Key 保存，现与其他模型配置一起写入本地 model.json，启动自动读取；留空保留同地址密钥，更换地址不沿用。持久化/恢复/切换地址针对性检查通过（0.364 秒），其余 11 项 REQ-005 检查通过；同步更新界面与当前指南，日志继续脱敏。

2026-09-17：步骤编写改为选择插件，保存时展开为选中插件全部当前动作；既有能力列表显示为插件选择，缺失插件保留不可用提示。动作插入自动选择所属插件，插件管理可展开动作说明及输入 schema。AI 生成若引用未授权动作，反馈精确动作清单并重新生成一次；仍失败显示具体动作，不绕过执行权限。6 项模型/编写/日志检查通过，真实界面回归待记录。

插件选择调整后的真实界面回归通过（1 项，93.299 秒），覆盖动作插入自动选择插件、生成、调试确认、执行、历史与日志窗口。工程与 diff 检查通过。

2026-09-17：真实 Chromium 新用例通过（13.491 秒），验证同任务五步调试共用 PID/页面、正式运行到第三步后继续到第五步只产生五次尝试、成功后仍保留进程、显式结束后资源释放且历史保持成功。完整 60 项检查（255.042 秒）中 58 项通过，包括真实 UI；两项测试在启动后修订导致运行进程仍使用旧测试内容：新增用例首步骤无业务输出，及旧模型桩少一次修正响应。修订后分别单独通过（13.491 秒、0.357 秒），实现未因此更改。日志在 .runtime/retained-browser-validation.log，不能将该轮日志标为完整通过。工程与 diff 检查通过。

2026-09-17：修复 DesktopController.call 的 operation 形参与 run.control 参数冲突，原按钮 TypeError 导致没有发出关闭命令。调用名称改为 api_operation。调试日志旁新增“AI 根据调试日志修复”：携带最近尝试错误及脱敏日志（最多 50 条/16KB），共用差异预览和采纳，不自动执行或固化。普通生成按钮也包含已有调试反馈和日志。真实浏览器失败关闭与界面回归验证中。

2026-09-17：真实浏览器超时失败后，经 DesktopController 关闭资源的检查通过（9.869 秒）；反馈包含最近尝试错误及日志。真实界面回归通过（91.967 秒）。按用户追加要求，AI 请求对话及回复正文打印到服务日志，仍不记录 Authorization 头，API Key 脱敏；7 项模型/编写/日志检查通过（1.475 秒），覆盖对话可见和认证头不泄露。该记录取代此前不记录模型正文的约定。

2026-09-17：修复重启执行器残留锁：持有协调器 OS 锁后清除旧 runtime_lease，正常退出也释放占用；历史运行与 UNKNOWN 尝试保留，不自动判定副作用。步骤反馈重开时从 DB 恢复最新调试，重新显示结束入口。新调试创建前检查占用，同任务同环境已知安全失败允许重新调试，未知副作用必须先结束/核对，不新增被阻塞的孤立调试记录。12 项 REQ-005 回归通过（18.068 秒），新增死 owner 残留锁重启检查通过（1.574 秒），结果提交恢复检查通过（1.803 秒）；工程及 diff 检查通过。

2026-09-17：按用户要求补齐当前调试轮次 AI 会话内存历史，按步骤隔离，真正启动新的调试时清空，后续修复请求携带本轮对话。日志修复先弹出补充说明框；采纳后立即保存 DRAFT 并刷新当前页保留原标签，下一次调试使用最新草稿。ctx.call 的关键字 action_id 与位置参数采用同样权限检查，动态动作名不允许。8 项模型/历史/权限/日志检查通过（1.803 秒），旧界面全流程通过（97.126 秒）；新增补充说明及落库界面检查正在执行。

2026-09-17：新增补充说明、修复建议采纳落库的真实界面检查最终通过（127.284 秒）；验证补充说明进入模型请求、采纳源码存入 steps 且状态为 DRAFT、日志实时追加。9 项模型/历史/权限/日志/最新草稿执行检查通过（3.048 秒）。首次新增界面检查最后因日志成功记录不再唯一而失败，调整测试定位器后通过；实现未因此更改。工程与 diff 检查通过。

2026-09-18：五项调整完整回归通过：69 项（282.904 秒），含真实 NiceGUI/Chromium 界面、新建执行及点击步骤继续、通用环境编辑、手动确认与本次执行重置隔离、Playwright 任务参数优先级。工程检查与 REQ-002 的 8 项设计检查通过，git diff --check 通过。日志见 .runtime/workbench-sept18-validation.log。未进行 Windows 便携包验证。

2026-09-18：正式执行按钮统一为“结束执行”，调试为“结束调试”。点击先确认，按执行定义快照与插件资源声明展示将释放的资源；Playwright 提供可选 resource_descriptions 说明浏览器/页面/上下文，其他插件无说明则显示资源 ID。仅关闭已创建资源，无资源则提示释放执行器。取消不发送控制命令，保存结果与历史保留。工程检查通过，4 项相关运行/重置/配置回归通过（3.647 秒）；本轮未重跑完整 UI 集成。

2026-09-18：日志窗口删除浏览器式下载入口，改为打开本地日志目录；新增复制当前显示日志（macOS pbcopy / Windows clip，Linux 浏览器剪贴板），清空与筛选同步维护显示缓冲，并开放文本选择。日志始终自动保存到 logs/server.log。工程检查与 9 项模型/日志回归通过（3.584 秒）；未验证 Windows 系统剪贴板或重跑原生窗口。

2026-09-18：按用户简化要求，移除“复制当前日志”按钮及系统剪贴板调用，仅保留“打开日志目录”；清空、级别筛选、暂停和可选中文字仍保留。

2026-09-18：普通 AI 生成/修订不携带或写入调试历史，也不自动附带调试反馈；日志修复显式 use_history=True，累计当前调试过程的修复对话。反馈页再次执行调试沿用上一调试环境和实际输入、保存并执行最新草稿，不弹环境确认且不清空历史。内容页发起新的调试仍重置调试历史，内存按步骤隔离。新增 use_history/continue_session 均可选，默认无历史/新调试，兼容旧接口。9 项相关模型/历史/草稿回归通过，工程检查通过。

2026-09-18：成功调试并验证后，新步骤采集保留的真实浏览器页面检查通过（17.729 秒）；应用重启清空观察会话但保留成功执行历史检查通过（1.214 秒）。工程与 diff 检查通过。首轮新增测试误建成不同 task 导致状态校验失败，修正同 task 后通过。

2026-09-18：修复失败调试阻挡修订：仅步骤保存允许空闲 FAILED/SUCCEEDED TRIAL 租约，不释放浏览器，不放开正式运行或执行中编辑锁。反馈页显式重试允许同任务环境失败调试租约移交到新尝试，保留旧 UNKNOWN/FAILED 证据；效果未知时提示用户检查页面并确认再次执行，不强制结束浏览器，记录 USER_RETRY_WITH_RETAINED_SESSION。真实 Chromium 超时失败→最新草稿修订→同 PID/页面再调试成功→显式结束检查通过（11.588 秒）。工程检查通过。

2026-09-18：调试反馈页新增“从首步开始”：确认环境/任务输入后关闭当前同任务调试资源，新建 flow_trial（TRIAL 的可选 request_json 标志，无 DDL 变更），执行第一步到当前步骤，使用正式绑定及数据库前序结果和间隔，失败停止，不要求预先成功验证；历史调试保留。普通反馈页继续调试仍保留页面。6 项相关回归通过（6.918 秒），包含草稿前序依赖及全新 worker 检查。

2026-09-18：按确认方案实施 Playwright 四项优化：可见控件快照/可靠定位与状态、输入值及 URL/标题/文字结果等待、动作开始/失败/耗时与失败快照、修复前自动刷新保留会话、插件专属提示词。升级插件到 0.2.0、API v1 不变、无 DDL 变更。新增真实 Chromium 连续搜索检查通过（11.097 秒）：前步打开页面→隐藏旧控件失败→新快照进入模型请求→修订使用可见控件→同 PID 输入点击及实际结果断言成功，再次采集为新时间及结果页面。原人工提交场景单独回归通过（31.552 秒）。完整回归执行中。

当前实现使用 128 KiB 本地请求上限。测试覆盖历史 `0`、最近两轮、不限制、静态字段去重开关、完整当前上下文超过旧 64 KiB 后仍原样保留，以及已选择历史超过上限时明确失败且不被静默删除。

最终连续搜索检查通过（9.481 秒），新增覆盖 READ 失败自动释放租约后仍保留页面、刷新上下文再修复；观察接口依据实际存活的 session_run_id，而非 DB 租约是否存在。快照最终限制与状态/标签优化已在此用例覆盖。模型/历史 10 项回归通过（6.001 秒），四轮大快照保留全部对话、内存原始快照且请求仅引用历史快照。

2026-09-18：Playwright 四项优化完整回归 72 项通过（339.693 秒），包含真实 NiceGUI/Chromium 界面、插件、运行/存储。运行中追加的历史快照引用优化另以模型/历史 10 项通过（6.001 秒），最终快照与无租约 READ 失败继续采集另以真实连续搜索用例通过（9.481 秒）。工程检查、设计 8 项检查及 diff 检查通过。浏览器搜索使用本机测试站点，AI 使用模型 fixture，未声称真实 DeepSeek/百度端到端成功。首次插件回归发现普通元素不可调用 is_editable、LOCATOR 诊断范围及旧测试误把 selector.value 当成业务输入值；修正后完整回归通过。

2026-09-18：结束按钮改为 run.get 的实时 can_end（当前存活 worker/session 或当前运行租约），不按 SUCCEEDED 历史状态推断。显式结束后的成功历史、未启动执行和重启后历史不显示按钮；保留会话或占用执行器时显示，调试结束入口同规则。核对结果仍需选择已完成/未完成，说明改为选填，后台允许空 evidence，不取消人工核对。7 项相关检查通过（22.133 秒），未知执行人工核对空说明后继续检查通过（2.637 秒）；工程与 diff 检查通过。无数据库迁移，can_end 是实时派生字段。

2026-09-18：运行历史名称与网页 AI 编写交付：显示检查 3 项通过；网页导出/回复 2 项、模型/历史 10 项、插件契约 5 项通过；真实 NiceGUI/Chromium 全流程（含网页导出、粘贴采纳、无额外模型请求及历史补充说明）通过，114.968 秒。变量调整后同一界面全流程再次通过，107.419 秒。

2026-09-18 验证：变量/步骤默认输入合并检查通过；核心 26 项通过（37.288 秒），模型与对话 10 项通过（3.412 秒）。原“拒绝本地密钥保存”检查按用户当前要求更新为允许配置、运行摘要仍脱敏，修正测试中的草稿未确认后通过。验证码 3 项通过（16.135 秒）：禁止 socket 网络连接仍实际识别 1234；浏览器真实采图→本地 OCR→填写→提交→URL/title 断言成功；当前上下文包含验证码图片及密码框定位，不含密码值。连续搜索/失败上下文修复额外回归通过（7.114 秒）。首次本机原生库导入约两分钟，完成初始化后复测通过；Windows 实机与真实业务验证码准确率不在本机 fixture 验收中。

2026-09-18 最终交付：本地变量和表单自动预填、独立保存任务配置、网页 AI 两个入口及回复采纳、插件渠道适配、历史名称、超长历史保留最近两轮、本地 ocr 插件与通用元素采图均已实施。变量 5 项（1.798 秒）、执行回归 7 项（6.324 秒）、网页/中文/超长历史 4 项（1.363 秒）通过；核心 26 项、模型 10 项、显示 3 项、插件契约 5 项、OCR 3 项、连续搜索 1 项及真实界面 1 项检查通过。工程检查覆盖 101 个 Markdown 文档并通过，diff 检查通过。实际 OCR 与浏览器登录使用本机测试图片/站点；未操作用户真实登录页，未验收 Windows 便携包。无 DDL 变更；Playwright 0.3.0、ocr 0.1.0、插件 API v1。

2026-09-20：网页 Chat 解析、预览、差异和采纳流程通过真实 NiceGUI/Chromium 全流程，116.050 秒；Web Chat、模型日志和工作台回归 25 项通过（11.462 秒）。工程检查和 diff 检查通过。

### 任务配置刷新回归验证（2026-09-18）

`tests/test_task_config_refresh.py`：连续打开、修改保存、用原页面的旧任务引用再次打开，验证名称、说明与参数结构均显示最新值，并保留步骤编辑对象、不重绘页面。1 项通过；工程结构检查与 `git diff --check` 通过。

### 调试环境、变量与紧凑列表（2026-09-18）

真实工作台浏览器验收 `test_req005_ui.py` 1 项通过（150.099 秒），覆盖步骤验证、紧凑步骤导航、执行行点击、历史、AI 修订、环境配置与服务日志。新增 `test_trial_variable_groups.py` 验证分组只读/可编辑边界、环境切换值更新并保留手动任务输入、反馈调试路由使用当前环境与输入，以及实际 worker 重试采用最新任务值。`test_variables.py` 5 项、`test_workbench_changes.py` 7 项通过。不同环境的活跃会话先经资源确认结束；应用重启后的历史调试不视为可复用浏览器会话。


### 分段重跑、人工输入和流程修复验证（2026-09-18）

`tests/test_runtime_inputs.py` 6 项通过（6.149 秒）：任务/步骤缺少必录参数在动作之前暂停、提交输入幂等、同一 worker 继续、独立调试读取历史成功字段、中间重跑保留前缀结果及 worker 并清理后续结果、不同环境创建执行、修改环境不撤销验证，以及从首步流程调试后导出本次失败日志。`test_trial_variable_groups.py` 与 `test_task_config_refresh.py` 联合相关核心检查 9 项通过（6.476 秒）；变量与执行回归联合 17 项通过（15.421 秒）。首次分段清理检查发现已删除结果引用仍持有外键，补充清理引用后通过。没有操作用户业务任务或真实登录环境。


真实 NiceGUI/Chromium 界面复验 `tests/test_input_ui.py` 1 项通过（75.000 秒）：绿底白字、标题后勾选、依赖字段实际下拉、隐藏固定值、环境单选创建执行、任务与步骤依次弹窗补录、成功步骤选择中间起点重跑并重新补录、详情属于执行行下方、任务列表删除入口、无页面错误及后台 Traceback。首次界面检查发现重复补录弹窗被已删除旧窗口状态挡住，增加存活检查后修正；一次复测点击早于异步步骤切换完成，测试增加等待实际步骤名称后通过。核心最新 6 项通过（6.793 秒），工程检查覆盖 101 个 Markdown 文档，diff 检查通过。界面与核心均使用隔离临时目录，没有清理用户真实任务。


### 执行删除、环境同步与三类参数补录（2026-09-18）

相关核心/表单检查 12 项通过（9.917 秒），包括仅删除指定执行及结果、保留任务和其他执行输出、补录弹窗读取本地原始输入而非不含 inputs_json 的公共详情、三类变量分组、分段清理/输入暂停和任务配置刷新。三类补录界面初次集成时发现公共运行详情不返回原始输入，增加本地执行输入读取后修复。执行列表外独立详情元素已删除，详情仅属于被选中的执行卡片，收起时删除对应元素。工作台、弹窗和日志窗口采用中性按钮色；成功/失败状态保留绿/红。无 DDL 变更，run.delete 为新增接口。


完整真实 NiceGUI/Chromium 复验 1 项通过（77.137 秒）：独立调试弹窗选择 UAT 后反馈页及重复调试使用 UAT；顶部只有一个结束测试按钮；成功调试按钮为绿；执行任务和步骤补录均展示三类变量，非当前分组只读；绿色步骤选择起点重跑；点击 × 删除后执行消失、页面不存在空白面板、普通按钮无原蓝色背景；无页面异常和服务 Traceback。曾因连续操作尚未结束导致结束点击被忽略，改为处理中统一禁用操作、完成后恢复并保留结束按钮有效状态后通过。依赖字段检查也覆盖实际结果下拉。工作台样式截图已人工检查。工程检查覆盖 101 个 Markdown 文档，diff 检查通过。


### 白底按钮、默认环境与去重补录验证（2026-09-18）

13 项核心/表单回归通过（15.107 秒）：默认环境本地保存后重读、联合提交任务和首步输入后直接成功不二次暂停、同一补录请求两次刷新只建一个弹窗、分段重跑和清理、独立执行删除、环境切换与配置刷新。完整真实工作台浏览器检查 1 项通过（80.409 秒），覆盖白底操作、绿底成功、红色删除、UAT 同步、三类参数补录及只读边界、重跑与执行删除；截图已人工检查，详情内部及执行行均有收起入口。工程检查覆盖 101 个 Markdown 文档，diff 检查通过。无 Windows 实机或真实业务登录验收。

### 2026-09-18 输入提示与横排按钮验证

- 参数、运行输入与配置刷新：13 项检查通过（14.016 秒）。
- 真实 NiceGUI + Chromium 检查通过（80.426 秒）：空值提示、三个调试操作横排、弹窗底部操作横排、浅青成功状态、UAT 环境及本次录入值保留、继续调试和结束操作。
- 页面证据：`.runtime/req005-ui-evidence/trial-actions.png`。
- 修正调试环境同步触发的重复表单刷新，避免清空刚录入的值。

- 单一验证按钮交互检查通过：覆盖成功证据、尚未调试、过期证据和取消确认四种情况（`tests.test_confirmation_ui`）。


### 2026-09-19 插件结果展示、TiDB 与任务分享

- 新增功能回归 28 项通过（25.178 秒）；核心 REQ-003 26 项通过（40.188 秒）。
- TiDB 真实 PyMySQL 协议驱动、独立步骤进程与浏览器失败截图回归 2 项通过（7.491 秒）。数据库使用隔离 MySQL 协议测试服务，尚未连接用户实际 TiDB。
- 完整 NiceGUI/Chromium 页面检查两次通过（83.628 秒、84.193 秒），报告表格截图已人工核对：固定详情文字、成功步骤查看入口、报告和表格页签、原始数据、任务 JSON 导出和粘贴导入、环境删除。
- 修复结束确认期间运行状态变化导致操作被拒绝的问题；结束前刷新状态，并处理执行恰好完成的竞争情况。
- 工程结构和源码检查通过（102 个 Markdown 文档），git diff 空白检查通过。无数据库结构变更。


2026-09-19 截图展示修复：引用解析、丢弃输出检查、结果持久化、REQ-003 核心和图片定位共 35 项通过（48.886 秒）。工程检查和 diff 检查通过。真实日志确认原步骤丢弃 ctx.output 返回值且 outputs=[]，历史未保存截图无法恢复；需修订后重新调试。


2026-09-19 任务清理验收：12 项检查通过（10.666 秒），包括清空运行及任务目录中的文件/备份/暂存、保留配置和已验证状态、其他任务结果不变、重复清理可用、清理后重新执行成功，以及执行中拒绝清理。工程检查及 diff 检查通过；未对用户真实任务执行清理。


2026-09-19 导出交互修复：6 项检查通过（2.976 秒），覆盖 UTF-8 JSON 实际落盘、重复导出独立文件、打开目录调用、macOS 剪贴板传参及失败处理、浏览器异步复制结果、任务导入导出回归。剪贴板及系统打开动作使用测试替身，未覆盖用户当前剪贴板；工程检查和 diff 检查通过。
### 2026-09-19 多插件上下文列表

- 上下文累积、跨插件顺序、逐条删除、API/Chat 对话及确认交互共 14 项检查通过（8.715 秒）。
- REQ-003、工作台和网页 Chat 相关核心回归 37 项通过（53.612 秒）。
- 工程结构、源码和依赖边界检查通过（102 个 Markdown 文档），diff 检查通过。
- 更新验收脚本中已过期的必录字段标签后，完整 NiceGUI/Chromium 流程 1 项通过（114.706 秒），已覆盖合并后的“AI 生成 / 修订”入口、API 选择、生成采纳、调试与保存。同时修复选择弹窗按钮被全局忙状态禁用而无法选择的死锁。

2026-09-20：网页 Chat 粘贴解析新增前后额外文字中的 JSON 提取、Python 语法和单入口校验，并防止纯 Python 中名为 `step_content` 的业务字段被误认为外层回复。网页对话与工作台检查 12 项通过（14.542 秒）；工程检查及 diff 检查通过。
