# REQ-008 · 条件、循环与嵌套流程

状态：未开始。依赖：REQ-002、REQ-003、REQ-004。

## 目标

按确定的规则选择分支、重复执行步骤、复用子流程，所有控制流执行均可定位、暂停和追溯。

## 范围

- 条件节点使用受限比较操作符和明确的默认分支，不执行任意代码表达式。
- 循环明确数据集合或条件、迭代变量、最大次数与超时；初版逐项顺序执行。
- 嵌套节点引用子任务 ID，显式绑定输入输出，校验引用循环并拒绝递归。
- 运行中用节点路径、迭代序号、子运行 ID 区分每次执行，支持进入子流程单步查看。
- 非选中分支显示为未选中而非失败；循环失败默认停止，重试不得重做已确认成功的副作用动作。

## 验收

- [ ] 条件真/假/缺失输入都能得到确定结果，缺失数据不静默转为默认分支。
- [ ] 循环零次、多次、上限及失败重试均可追溯，输入集合在运行开始时固定。
- [ ] 嵌套任务参数隔离且输出可回传；循环引用和不存在子任务在执行前拒绝。
- [ ] 中途暂停、退出、恢复能定位同一迭代/子运行，不将 UNKNOWN 自动重试。

[设计](design.md) · [代码映射](code-mapping.md) · [计划](plan.md) · [进度](progress.md) · [验证](validation.md)

- [ ] 验证新增定义的导入导出、旧任务兼容、持久化升级及基础版本回归，更新交付说明。
